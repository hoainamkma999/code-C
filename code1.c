#include<stdio.h>
#include<math.h>
int main(){
int  a,b,c,d;
printf("ban hay nhap vao so a :");
scanf("%d",&a);

b=a*a;
c=a*a*a;
d=a*a*a*a;
printf("day la ket qua cua ban : %d %d %d ",b,c,d);
}